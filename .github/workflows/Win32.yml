name: Win32

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Add MSBuild to PATH
      uses: microsoft/setup-msbuild@v1.0.2

    - name: Restore NuGet packages
      run: nuget restore player-win32/fanplayer.sln

    - name: Build
      run: msbuild /m /p:Configuration=Release player-win32/fanplayer.sln
